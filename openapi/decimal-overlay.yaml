overlay: 1.0.0
info:
  title: Use faqturo.Decimal for exact decimal numbers
  version: 1.0.0
actions:
  - target: $.components.schemas..[?(@.type == 'number' && @.format == 'decimal')]
    update:
      x-go-type: Decimal
